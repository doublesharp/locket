schema_version = 1

[commands.deploy]
shell = "printf '%s\n' \"$DATABASE_URL\""
required_secrets = ["DATABASE_URL", "API_TOKEN"]
optional_secrets = []
allowed_secrets = ["DATABASE_URL", "API_TOKEN"]
env_mode = "strict"
override = "preserve"
ttl = "1s"
