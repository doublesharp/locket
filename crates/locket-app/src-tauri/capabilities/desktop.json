{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "desktop-default",
  "description": "Deny-by-default capability set for the Locket desktop shell. No fs, shell, http, updater, clipboard, dialog, or notification permissions are granted.",
  "windows": ["main"],
  "permissions": []
}
